## 1. Direct Cargo tag-floor snapshot

- [x] 1.1 Replace the naive Cargo rust-version line scan with table-aware direct-field parsing in `Update/Cargo/Msrv.hs`. Parse normalized `[package].rust-version` first and direct `[workspace.package].rust-version` second; preserve explicit absence; fail on malformed TOML or malformed present values. Treat valid `rust-version.workspace = true` as an absent direct value and do not resolve it. Cover comments/formatting, wrong-table keys, package-vs-workspace precedence, inheritance marker, absent fields, malformed values, and numeric `1.100 > 1.99` behavior. Verify focused ecosystem/MSRV tests.
- [x] 1.2 Consolidate the Cargo tag probe to one callback-based package -> lock -> repository implementation with effective package path `cargoPackageSubdir <|> cargoLockSubdir` and stable path deduplication. Expected missing paths continue fallback; transport/auth/server and parse errors fail closed. Remove duplicate probe logic without introducing a `Update.Cargo.Msrv` -> `Update.Deps.Plan` import cycle. Verify ordered fetch, deduplication, fallback, and failure tests.
- [x] 1.3 Extend the runtime-lane plan (or equivalent selected-PV structure) to retain each selected Cargo PV's raw direct tag-floor snapshot separately from the candidate-selection `"0.0.0"` fallback. Candidate selection uses the fallback only when direct absence is complete; donor/template floors do not affect lane selection. Ensure materialize-image Rust floor lookup consumes the selected declared floor and does not mistake absent-as-`0.0.0` for an ebuild write floor. Verify Cargo lane tests, including donor `1.95` not changing candidate selection from tag floor `1.91`.
- [x] 1.4 Serialize selected Cargo tag-floor snapshots inside the existing `DepsPayload`; do not add a separate persisted `FloorPayload`. Treat pre-change Cargo deps entries without snapshots as misses while preserving otherwise-valid non-Cargo entries. Include tag prefix, Cargo package/lock paths, and a floor-policy/parser version in Cargo deps-plan validity. Verify cache JSON round-trip, old-Cargo miss, non-Cargo compatibility, policy-key invalidation, and zero Cargo.toml re-fetch after a valid hit.

## 2. Canonical ebuild and template selection

- [x] 2.1 Add internal leaf module `Update.EbuildSelection` (`other-modules`, not exposed) with one canonical selector: filter live ebuilds, compare bare PV numerically, and choose the highest Gentoo revision numerically (bare revision zero). Fail closed for relevant incomparable candidates instead of using input/list-directory order. Unit-test bare/`-r2`/`-r10` in arbitrary order, live `9999` exclusion, unrelated PV exclusion, and incomparable input.
- [x] 2.2 Use the canonical selector in check and the direct apply entry so both assess the same highest-revision same-PV file. Remove first-match discovery and unsorted revision-tolerant scans. Verify identical assessment under permuted discovery/directory order.
- [x] 2.3 Capture canonical same-PV paths and the highest non-live cross-PV fallback in `PlannedDeps` or an equivalent initial-inventory selection structure, with an `Either`/error path for incomparable relevant candidates. Change `findTemplate` to consume that planned selection and return canonical path plus same-PV flag; do not let later directory rescans replace the cross-PV fallback. Update all three callers: full Cargo materialization, Cargo reuse, and `overlayAfterAssets`. Verify same-PV priority, lower/upper cross-PV backfill fallback, live exclusion, error propagation, and a multi-PV run where the first write must not become the second unit's fallback.

## 3. Shared adequacy assessment and full-path routing

- [x] 3.1 Create internal `Update.Adequacy` (`other-modules`) with `ContentAssessment` carrying `needsWorkPVs` and `forceFullPVs`, plus present-PV outcomes `Adequate | NeedsRewrite | NeedsFull`. It consumes canonical ebuild content and planned requirement snapshots and performs no upstream fetch. Centralize `requiredAssetBasenames :: PackageKey -> EcosystemSpec -> Text -> Text -> [FilePath]`. Keep dependencies cycle-safe and verify `cabal build all`.
- [x] 3.2 Refactor `Update.Check`/outdated assessment to use canonical ebuild selection and `Update.Adequacy`, removing Cargo tag re-fetch from `contentFixPVs`. Cargo with neither planned tag floor nor valid canonical same-PV floor is needs-work plus force-full; a written `1.95` against tag `1.91` is adequate. Add `usage` steady-state, missing/malformed local floor, no-signal full-route, highest-revision, and cache-hit/no-reprobe tests in `test/Test/CheckPlan.hs` and focused predicate tests.
- [x] 3.3 Refactor update planning to store both assessment sets and initial-inventory selections in `PlannedDeps`, propagating selection/assessment errors as `FetchError`, `PlanHardFail`, or `ApplyHardFail` rather than arbitrary ordering. Production apply consumes the sets without content reclassification; Cargo also consumes the selected tag snapshot without refetch. Refactor the direct plan-and-apply entry to invoke the same assessment once. Preserve the legacy Go-only `contentFixNeeded` wrapper returning needs-work PVs. Verify plan/direct/apply parity and error propagation from shared fixtures.
- [x] 3.4 Make forced-full and release-state routing authoritative in `classifyPackageUnits`: complete release + not forced -> `ExpectedReuse`; no release -> `ExpectedFull`; existing partial release or any existing release + forced-full -> package hard-fail with owner/repo/tag guidance. Carry admitted per-PV routes alongside plans through `Update.Spine`, disk/preflight, materialize-image admission, normal mutation, and withheld-wave reclassification. Do not discard `ClassifiedPvUnit` routes after preflight. Verify hard-failed packages are excluded from disk-gate math.
- [x] 3.5 Compute reuse-floor feasibility during planning for every missing or content-fix Cargo PV from the planned tag floor plus canonical selected template. A valid cross-PV template permits conservative reuse; no valid operand adds the PV to force-full. Distinguish missing declarations from propagated fetch/parse failures. Verify no-release forced-full can proceed to full, while an existing tag for the same forced-full unit hard-fails before materialization.
- [x] 3.6 Plumb release lookup dependencies and configured assets repository identity into outdated reporting so `[assets reusable]` is emitted only after confirming every required asset and non-forced status, for missing-PV and same-PV gaps. Lookup unavailability/error must retain the needs-work line and suppress the optional marker. Verify complete, partial, missing, forced-full, and lookup-error output without turning marker lookup failure into package check failure.
- [x] 3.7 Make mutation consume and revalidate each admitted route immediately before the PV unit. `ExpectedReuse` must remain complete and `ExpectedFull` must remain tag-absent; mismatch or lookup error hard-fails rather than switching paths after disk/image admission. Initial conflicts fail before image admission; recheck conflicts fail before that unit's mutation. Preserve existing diagnostics for an external tag-creation race after recheck and preserve best-effort deletion only for a release created by the current failed upload. Verify normal and withheld-wave route propagation plus reuse-deleted/full-tag-created races.

## 4. Cargo decision, build, and reuse floors

- [x] 4.1 Implement the decision floor as numeric `max(planned tag floor, canonical same-PV donor floor)` and change `ebuildNeedsCargoContentFix` to too-low-only comparison. Missing/malformed written `RUST_MIN_VER` needs repair; absence of both decision operands is `NeedsFull`. Test equal, lower, higher, malformed, absent, and multi-digit numeric versions.
- [x] 4.2 Update clone harvest to use the table-aware direct parser while retaining the recursive clone/source-tree walk and existing build/cache directory exclusions. Add registry harvest that enumerates immediate `stage/cargo_home/gentoo/{name}-{version}/Cargo.toml` package roots only. Test clone `1.91` plus registry root `1.95`, nested registry example `1.99` exclusion, malformed manifest failure, and absent declarations.
- [x] 4.3 Wire full-path build floors as bump `max(tag, clone harvest, registry harvest)` and same-PV `max(tag, canonical same-PV donor, clone harvest, registry harvest)`. Never use the cross-PV fallback or pycargo's working-ebuild `RUST_MIN_VER` as a bump/harvest operand. Keep the tag plan snapshot authoritative rather than performing another tag fetch. Verify harvest raise, stale-donor drop on full bump, same-PV monotonicity, and root-above-donor behavior.
- [x] 4.4 Hard-fail a full-path Cargo unit when all applicable operands remain absent after pack/harvest. Ensure failure occurs before overlay ebuild/Manifest mutation, release publication, or commit and retains the failed work unit per existing diagnostics. Update error text to name direct tag, clone/registry harvest, and same-PV donor facts. Verify no-write/no-publish failure coverage.
- [x] 4.5 Implement reuse write as numeric `max(planned tag floor, canonical selected template floor)` with no tag re-fetch, clone, pycargo, or harvest. Verify same-PV and cross-PV template reuse, tag-above-template, sticky template-above-tag, and that a conservative-high reuse result is accepted without scheduling automatic correction.
- [x] 4.6 Pass the computed full/reuse floor to `OverlayWrite.ensureRustMinVer` as the single intended assignment, removing every duplicate direct assignment from the template. Add end-to-end coverage where pycargo leaves donor `1.95` in the working body but a full bump computes `1.91`; assert the final overlay ebuild contains exactly one `RUST_MIN_VER="1.91.0"`. Also verify duplicate cleanup and that a staged registry `1.95` write immediately passes shared adequacy.

## 5. Exact Manifest and all-assets completeness

- [x] 5.1 Replace substring Manifest matching with one cycle-safe exact `DIST` record module: first token exactly `DIST`, second token exactly the required basename. Share it across adequacy, post-write SHA512 selection, `Update.DiskSpace`, and preflight consumers rather than retaining parallel token parsers. Reject conflicting duplicate exact records. Test prefix/suffix/`.asc` collisions, `DISTANCE`, duplicate conflicts, primary/companion names, SHA selection, and disk baselines.
- [x] 5.2 Repoint check, update planning, outdated release-marker lookup, release classification, direct apply, and materialization to the centralized `requiredAssetBasenames`. Preserve unit-wide reuse: every primary and companion asset must exist and be usable, forced-full always wins, no release permits full, and any existing non-reusable release hard-fails update without automatic remote mutation. Keep `materializeCompanionAssets` generation synchronized. Verify partial opencode release hard-fails update and receives no `[assets reusable]`, while a complete non-forced release can reuse all assets.
- [x] 5.3 Compute reuse disk baselines from the complete required asset set, not the first asset. Sum each basename's positive API size, otherwise its exact Manifest size, otherwise the reuse ecosystem floor for that unresolved basename, then apply reuse factor/margin rules. Verify all-API, mixed API/Manifest, and unresolved companion-size estimates.
- [x] 5.4 Add one cross-path opencode fixture with adequate canonical ebuild content but a missing exact models Manifest record. Verify outdated and update planning report work, direct assessment agrees, a sidecar does not satisfy it, and a complete non-forced release can rewrite/verify every required record. Verify an existing partial release instead hard-fails update before admission under the selected no-repair policy.

## 6. Cleanup, documentation, and gates

- [x] 6.1 Remove superseded Cargo fetch/classifier code (`fetchCargoMsrv` in `Update/Check.hs`, `fetchCargoMsrvForPV` and first-match helpers in `Update/Apply/Materialize.hs`) and duplicate `requiredAssetBasenames` definitions. Remove or rename naive parsing helpers replaced by table-aware parsing. Keep clone harvest helpers and numeric floor combinators that remain live. Update cabal `other-modules`; do not broaden `exposed-modules` or weeder roots. Clear stale `.hie` before the final build if module changes leave stale interfaces.
- [x] 6.2 Update affected test constructors/JSON fixtures for the planned Cargo snapshot and forced-full sets. Confirm the existing non-Cargo Go/Npm/Bun/Sbcl planning, cache, image-floor, reuse, and apply behavior remains green; this change must not alter their runtime floor rules.
- [x] 6.3 Per `project-docs`, confirm no README/CONTRIBUTING/AGENTS change is needed because there is no CLI/config/tooling change. Do not manually copy delta specs into living `openspec/specs/`; OpenSpec sync/archive owns that merge. Verify no operator documentation claims clone-only or pycargo-derived Cargo MSRV behavior.
- [x] 6.4 Run `openspec validate cargo-floor-parity --strict --json` and resolve every issue. Confirm the change still contains deltas for `cargo-crates-assets`, `deps-assets`, `outdated-command`, `check-cache`, `assets-publish`, `disk-space-preflight`, and `go-vendor-assets`.
- [x] 6.5 Run the full required quality gate `hk check` (including build, coverage-enabled tests, formatting, hlint, stan, and weeder) and confirm exit success before marking implementation complete.
